spring:
  application:
    name: user-aggregator-test

server:
  port: 8080

springdoc:
  api-docs:
    path: /v3/api-docs

data-sources:
  databases:
    - db-name: data-base-1
      strategy: postgres
      url: ${test.postgres.url}
      user: ${test.postgres.user}
      password: ${test.postgres.password}
      table: users_pg
      column-mapping:
        id: user_id
        username: login
        name: first_name
        surname: last_name
    - db-name: data-base-2
      strategy: mysql
      url: ${test.mysql.url}
      user: ${test.mysql.user}
      password: ${test.mysql.password}
      table: users_mysql
      column-mapping:
        id: id
        username: username
        name: name
        surname: surname
